- Benidorm

Done:
- [x] Find out why the about page is broken — en/about.html loaded "sharesd.js" (typo); fixed to shared.js
- [x] More filters behind a "More filters" button — area range + price range (currency-aware) added
- [x] Improve overall flow and looks of the site, especially the landing page — new hero tagline,
      category tiles with live counts, data-driven featured grid, contact strip, reveal animations,
      auto-hiding header
- [x] Software to enter listing data instead of hard-coding — see ../estate_manager.pyw
      (all property data now lives in v1/data/properties.js; HU/EN pages render from it,
      IDs unified across languages so featured deep-links work on both)
